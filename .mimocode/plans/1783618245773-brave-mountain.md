# Plan: Add 3 Tutorial Sentences to Localization

## Context

User wants three new tutorial sentences in Ukrainian for their puzzle game, integrated into the existing localization system (CSV + enum).

User-provided text (needs polish):
1. "переміщай горизонтально смуги потягнувши за сегменти"
2. "в спеціальних зонах переміщай сегменти між смугами"
3. "щоб виграти збери всі сумги в один колір" (typo: "сумги" → "смуги")

## Polished Text

| Key | Ukrainian | English |
|-----|-----------|---------|
| `Tutorial_DragStrips` | Переміщуй смуги горизонтально, потягнувши за сегменти | Swipe strips horizontally by dragging segments |
| `Tutorial_SlideBetweenStrips` | У спеціальних зонах переміщуй сегменти між смугами | In special zones, drag segments between strips |
| `Tutorial_WinGoal` | Щоб виграти, збери всі смуги в один колір | To win, collect all strips into one color |

## Changes

### 1. `Assets/Feature/LocalizationModule/Scripts/Data/LocalizationKey.cs`
Add 3 new enum entries after `Tutorial_TrySlideHere = 39`:
```csharp
Tutorial_DragStrips = 40,
Tutorial_SlideBetweenStrips = 41,
Tutorial_WinGoal = 42,
```

### 2. `Assets/Feature/LocalizationModule/Resources/Localization/Localization.csv`
Append 3 new rows (Id 40, 41, 42) with translations for all 11 languages (Ukrainian, English, Polish, German, French, Spanish, Italian, Portuguese, Turkish, Arabic, Hindi).

## Verification

1. Open Unity Editor — should compile without errors
2. Check `LocalizationKey` enum has entries 40–42
3. Check CSV has rows 40–42 with all 11 languages populated
