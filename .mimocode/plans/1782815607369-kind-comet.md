# Plan: Place analytics event calls

## Insertion map

| Event | File | After what |
|-------|------|------------|
| LevelStarted / DailyChallengeStarted / PerfectMapLevelStarted | `LevelInitializeService.Initialize()` | after `levelData` is resolved (line ~101) |
| LevelCompleted | `LevelResultService.OnLevelWon()` | non-challenge branch, after `SaveLevelCompletion` |
| DailyChallengeCompleted | `LevelResultService.OnLevelWon()` | challenge branch, after `_challengeService.OnChallengeCompleted(result)` |
| UndoMoveUsed | `UndoTool.Execute()` | after `_undoService.Undo()` |
| ExtraMovesPurchased | `LosePresenter.AddMovesButtonClick()` | inside `OnConfirmSuccess` after `_moveTrackService.AddMoves(...)` |
| PerfectMapLevelStarted | `LevelInitializeService.Initialize()` | same block as LevelStarted, branching on `_levelModel.ReplayLevel` |

## Level ID resolution

Common pattern: `_levelModel.ReplayLevel ?? _saveDataModel.Get<PlayerProgressData>(SaveDataType.PlayerProgress).Level`
In LevelInitializeService, `levelData.LevelID` is available after line 101.

## Files to modify
1. `Assets/Feature/LevelInitializeModule/LevelInitializeService.cs` — add IAnalyticsService, fire start events
2. `Assets/Feature/LevelResultModule/Scripts/LevelResultService.cs` — add IAnalyticsService, fire completion events
3. `Assets/Feature/ToolModule/Scripts/Tools/UndoTool.cs` — add IAnalyticsService + LevelModel + ISaveDataModel
4. `Assets/Feature/LoseViewModule/Scripts/LosePresenter.cs` — add IAnalyticsService + LevelModel + ISaveDataModel
5. `Assets/Feature/DailyChallengeStartViewModule/Scripts/DailyChallengeStartPresenter.cs` — no change needed (start event fires in LevelInitializeService)
